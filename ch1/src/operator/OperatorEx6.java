package operator;

public class OperatorEx6 {
    public static void main(String[] args) {
        // 대입 연산자 : =
        // + 복합 대입 연산자 : +=, -=, /=, *=, %=
        int i = 3;
        i += 5; // i = i + 5;
        i -= 2; // i = i - 2;
        i *= 3; // i = i * 3;
    }
}
